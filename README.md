# Habit Reminder API

Backend-часть SPA веб-приложения для трекинга полезных привычек, основанного на концепциях книги «Атомные привычки» Джеймса Клира.

## 📋 Описание проекта

Сервис позволяет пользователям создавать, отслеживать и получать напоминания о полезных привычках. Каждая привычка следует формуле:

> **я буду [ДЕЙСТВИЕ] в [ВРЕМЯ] в [МЕСТО]**

Система поддерживает:
- Полезные привычки с вознаграждением или связанными приятными привычками
- Автоматические напоминания через Telegram
- Публичные привычки для обмена опытом
- Периодичность выполнения от 1 до 7 дней

## 🚀 Технологический стек

- **Backend**: Django 5.2, Django REST Framework
- **База данных**: PostgreSQL
- **Аутентификация**: JWT (Simple JWT)
- **Очередь задач**: Celery + Redis
- **Telegram бот**: aiogram + FastAPI
- **Документация**: drf-yasg (Swagger/Redoc)
- **Тестирование**: Django Test Framework, Coverage
- **Качество кода**: Flake8, Black, isort

## 📋 Требования к окружению

- Python 3.12+
- PostgreSQL 12+
- Redis 6+
- Poetry (для управления зависимостями)

## 🛠️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/ViktorShadr/habit-reminder-api.git
cd habit-reminder-api
```

### 2. Быстрый запуск в Docker (рекомендуется)

После настройки `.env` проект можно запустить **одной командой**:

```bash
docker compose up -d --build
```

Что поднимется в контейнерах:
- Django (gunicorn)
- PostgreSQL
- Redis
- Celery Worker
- Celery Beat
- Telegram bot
- Nginx

Проверка состояния сервисов:

```bash
docker compose ps
```

Остановка:

```bash
docker compose down
```

### 3. Установка зависимостей (локальный запуск без Docker)

```bash
# Установка Poetry (если не установлен)
curl -sSL https://install.python-poetry.org | python3 -

# Установка зависимостей проекта
poetry install --no-root
```

### 4. Настройка переменных окружения

Скопируйте файл примера и заполните его:

```bash
cp .env.example .env
```

Обязательные переменные в `.env`:

```env
# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# База данных
DB_NAME=habit_reminder
DB_USER=postgres
DB_PASSWORD=your-password
DB_HOST=localhost
DB_PORT=5432

# Redis для Celery
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Telegram бот
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_BOT_SECRET=your-secret-key
TELEGRAM_API_BASE_URL=http://127.0.0.1:8001
BACKEND_BASE_URL=http://127.0.0.1:8000
```

### 5. Создание и применение миграций

```bash
poetry run python manage.py makemigrations
poetry run python manage.py migrate
```

### 6. Создание суперпользователя

```bash
poetry run python manage.py createsuperuser
```

### 7. Запуск сервисов

#### Запуск Redis (в отдельном терминале)
```bash
redis-server
```

#### Запуск основного приложения Django (в отдельном терминале)
```bash
poetry run python manage.py runserver
```

#### Запуск Celery Worker (в отдельном терминале)
```bash
poetry run celery -A config worker --loglevel=info
```

#### Запуск Celery Beat (в отдельном терминале)
```bash
poetry run celery -A config beat --loglevel=info
```

#### Запуск Telegram бота (в отдельном терминале)
```bash
cd telegram_bot
poetry run python -m telegram_bot.run_bot
```

## 📚 Документация API

После запуска сервера документация доступна по адресам:

- **Swagger UI**: http://127.0.0.1:8000/swagger/
- **ReDoc**: http://127.0.0.1:8000/redoc/

## 🔌 Основные эндпоинты

### Аутентификация
- `POST /api/users/login/` - Вход пользователя
- `POST /api/users/token/refresh/` - Обновление токена
- `POST /api/users/register/` - Регистрация пользователя

### Управление привычками
- `GET /api/habits/` - Список привычек текущего пользователя (пагинация по 5)
- `POST /api/habits/` - Создание новой привычки
- `GET /api/habits/{id}/` - Получение привычки
- `PUT /api/habits/{id}/` - Обновление привычки
- `DELETE /api/habits/{id}/` - Удаление привычки
- `GET /api/habits/public/` - Список публичных привычек

### Управление профилем
- `GET /api/users/detail/me/` - Получение данных пользователя
- `PUT /api/users/detail/me/` - Обновление данных пользователя
- `DELETE /api/users/detail/me/` - Удаление аккаунта

### Интеграция с Telegram
- `POST /api/users/telegram/link/` - Получение кода для привязки
- `POST /api/users/telegram/confirm/` - Подтверждение привязки (внутренний)

## 🧪 Тестирование

### Запуск тестов
```bash
# Все тесты
poetry run python manage.py test

# С покрытием
poetry run python -m coverage run --source='.' manage.py test
poetry run python -m coverage report --include="habits/*,users/*" --omit="*/migrations/*"

# HTML отчет покрытия
poetry run python -m coverage html --include="habits/*,users/*" --omit="*/migrations/*"
```

### Проверка качества кода
```bash
# Форматирование
poetry run black habits users
poetry run isort habits users

# Проверка стиля
poetry run flake8 habits users --exclude=migrations --max-line-length=119
```

## 📊 Правила валидации

Система применяет следующие правила к привычкам:

1. **Время выполнения**: не более 120 секунд
2. **Периодичность**: от 1 до 7 дней
3. **Приятные привычки**: не могут иметь вознаграждение или связанную привычку
4. **Полезные привычки**: могут иметь либо вознаграждение, либо связанную приятную привычку
5. **Связанные привычки**: только привычки с признаком "приятная"

## 🔐 Безопасность

- JWT аутентификация с коротким временем жизни access токена (5 минут)
- Защита от CORS для фронтенда
- Проверка прав доступа к привычкам
- Секретный ключ для внутреннего API Telegram бота
- Валидация данных на уровне сериализаторов

## 📈 Тестовое покрытие

Текущее покрытие кода тестами: **95%**

- `habits/` - 96%
- `users/` - 89%
- Все критические бизнес-правила покрыты тестами на 100%

## 🏗️ Архитектура проекта

```
habit-reminder-api/
├── config/                 # Конфигурация Django
├── habits/                 # Приложение привычек
│   ├── models.py          # Модели привычек
│   ├── serializers.py     # Сериализаторы и валидация
│   ├── views.py           # View-классы
│   ├── services.py        # Бизнес-логика
│   ├── tasks.py           # Задачи Celery
│   └── tests/             # Тесты
├── users/                  # Приложение пользователей
│   ├── models.py          # Модель User и TelegramLink
│   ├── serializers.py     # Сериализаторы
│   ├── views.py           # View-классы
│   ├── services.py        # Сервис Telegram
│   └── tests/             # Тесты
└── telegram_bot/           # Telegram бот
    ├── main.py            # Логика бота
    ├── api.py             # FastAPI эндпоинты
    └── run_bot.py         # Запуск бота
```


## ⚙️ CI/CD и деплой

В репозитории настроен GitHub Actions workflow: `.github/workflows/ci.yml`.

### Что делает pipeline

1. **Тесты и линтинг** (для `push` в `develop/main` и `pull_request` в `develop`):
   - flake8
   - black --check
   - isort --check-only
   - Django tests + coverage
2. **Проверка Docker-сборки**:
   - сборка backend-образа
   - сборка образа Telegram-бота
3. **Публикация образов в GHCR** (только при `push` в `main`)
4. **Автодеплой на сервер по SSH** (только при `push` в `main`)

### Необходимые GitHub Secrets

- `SERVER_HOST`
- `SERVER_USER`
- `SERVER_SSH_KEY`
- (опционально для приватных настроек) переменные окружения, используемые на сервере в `.env`

### Как настроить сервер

1. Установить Docker и Docker Compose plugin.
2. Подготовить директорию проекта, например `/opt/projects/habit-reminder-api`.
3. Скопировать в нее `docker-compose.yml`, `nginx/` и файл `.env`.
4. Убедиться, что в `.env` заданы production-переменные (`SECRET_KEY`, `DB_PASSWORD`, `ALLOWED_HOSTS`, токены Telegram и т.д.).
5. Добавить публичный SSH-ключ GitHub Actions (соответствующий `SERVER_SSH_KEY`) в `~/.ssh/authorized_keys` пользователя деплоя.

### Что делает деплой

При успешном пайплайне на `main` workflow выполняет на сервере:

```bash
cd /opt/projects/habit-reminder-api
docker compose pull
docker compose up -d --remove-orphans
docker image prune -f
docker compose ps
```

Это обновляет контейнеры до последних образов и перезапускает сервис без ручного вмешательства.

## 🌐 Адрес развернутого приложения

- Основной адрес: **http://158.160.1.66/habit/**

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature-branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📝 Лицензия

Проект распространяется под BSD License.

## 📞 Контакты

Автор: Viktor Shadrin  
Email: v.viktor.shadrin@gmail.com

---
